<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>

	<entry key="Login">
		SELECT user_id,name
		FROM users
		WHERE user_id = ?
		AND password = ?
	</entry>

</properties>