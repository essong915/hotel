<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>

	<!-- FAQ 목록 조회 (카테고리 조건은 DAO에서 동적으로 추가) -->
	<entry key="faqlist">
		SELECT faq_no,
		category,
		question,
		answer,
		sort_order,
		status,
		reg_date
		FROM faq
		WHERE status = 'ACTIVE'
	</entry>

</properties>